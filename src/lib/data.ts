import type { GalleryImage } from '@/lib/types';

// Ya no se necesitan datos estáticos aquí, se cargarán desde la BD.
// Puedes mantener este archivo si planeas tener otros datos estáticos en el futuro,
// o eliminarlo si no es necesario.

// export const galleryImages: GalleryImage[] = [ ... ]; // Eliminado
